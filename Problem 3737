class Solution {
public:
    int countMajoritySubarrays(vector<int>& nums, int target) {
        int n = nums.size();
        int ans = 0;

        for (int i = 0; i < n; i++) {
            int cnt = 0;
            for (int j = i; j < n; j++) {
                if (nums[j] == target) {
                    cnt++;
                }
                int a = j - i + 1;
                if (cnt > a / 2) {
                    ans++;
                }
            }
        }
        return ans;
    }
};

⏱️ Time Complexity

O(n²) → because we check all possible subarrays.

Works well for small arrays but not optimal for large n.

💡 Example

Input:
nums = [1,2,2,2,3], target = 2

All subarrays where 2 is majority:
[2], [2,2], [2,2,2], [2,2,2,3], [2,2], [2,2,3] → 6 total

Output:
6
